import { StyleSheet } from "react-native";

const signUp = StyleSheet.create({
  text: {
    flexDirection: "row",
    alignSelf: "center",
    marginBottom: 30
  },

  link: {
    fontWeight: "bold",
    marginLeft: 5
  }
});

export default signUp;
